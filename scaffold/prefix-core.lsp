;;; ---------------------------------------------------------------------
;;; prefix-core.lsp
;;;
;;; The main logic for the "prefix" routine: whatever it actually
;;; calculates or draws, kept separate from the command entry point in
;;; prefix-command.lsp so the "front door" of the routine stays easy to
;;; find.
;;; ---------------------------------------------------------------------

(defun prefix-run (/ )
  ;; TODO: replace with the routine's real behavior. Use prefix-example
  ;; (or other helpers) from prefix-util.lsp as building blocks.
  (princ "\nprefix-run: not implemented yet.")
  (princ)
)
